<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hotel">

    <uses-permission android:name="android.permission.android: INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hotel">
        <activity android:name=".Profile" />
        <activity android:name=".Filters.ShowPeopleRooms" />
        <activity android:name=".Filters.ShowMoneyRooms" />
        <activity android:name=".Filters.ShowCategoryRooms" />
        <activity android:name=".ReservationA" />
        <activity android:name=".ShowRooms" />
        <activity android:name=".ViewForReservation" />
        <activity android:name=".Menu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>